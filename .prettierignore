node_modules
*.log*
.nuxt
.nitro
.cache
.output
.env
dist
# Генерируется npm — переформатировать нельзя, иначе каждый npm install даёт диф
package-lock.json

# Данные лендинга: правятся руками и дописываются json-server, отступ в два пробела.
# Прогон prettier переписал бы весь файл ради нулевой пользы
db.json
